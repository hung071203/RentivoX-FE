export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-zinc-50">
      <div className="w-full max-w-md p-8 bg-white rounded-lg shadow-sm border">
        <h1 className="text-2xl font-bold mb-2">RentivoX</h1>
        <p className="text-muted-foreground text-sm mb-6">Đăng nhập để tiếp tục</p>
        {/* LoginForm sẽ được thêm sau */}
      </div>
    </div>
  )
}
